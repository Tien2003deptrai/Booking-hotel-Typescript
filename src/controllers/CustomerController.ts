import { RequestHandler } from "express";

class CustomerController {
    customerLogin: RequestHandler = async (request, response) => { }

    customerRegister: RequestHandler = async (request, response) => { }
}

export default new CustomerController();

