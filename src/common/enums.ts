export enum HttpStatus {
    Error = 400,
    ErrorServer = 500,
    Success = 200
};

export enum HttpMessage {
    Error = 'Query Error',
    ErrorServer = 'Internal Server Error',
    Sucess = 'Server Response Success'
};